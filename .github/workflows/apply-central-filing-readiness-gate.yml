name: Central filing readiness gate staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Product files are updated directly; this staging workflow will be removed."
