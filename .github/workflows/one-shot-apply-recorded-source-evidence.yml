name: One-shot apply recorded-source evidence fix

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: read

# Completed successfully against #440. Kept temporarily as an auditable record
# until the QA stack is merged; the job is deliberately disabled and this PR is
# never a product merge candidate.
jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Recorded-source evidence repair already applied to #440.'
