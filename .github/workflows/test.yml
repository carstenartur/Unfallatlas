name: One-shot runtime split seam repair

on:
  pull_request:
    branches: [main]
    types: [opened]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  repair:
    if: startsWith(github.head_ref, 'automation/fix-runtime-split-seam-trigger')
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - name: Load and execute reviewed repair script
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin automation/fix-runtime-split-seam
          git show origin/automation/fix-runtime-split-seam:scripts/fix-runtime-split-seam.sh > /tmp/fix-runtime-split-seam.sh
          bash -n /tmp/fix-runtime-split-seam.sh
          bash /tmp/fix-runtime-split-seam.sh
