name: Apply PDF evidence page fix

on:
  push:
    branches:
      - agent/fix-pdf-evidence-page-contract

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate workflow startup
        run: echo workflow-started
